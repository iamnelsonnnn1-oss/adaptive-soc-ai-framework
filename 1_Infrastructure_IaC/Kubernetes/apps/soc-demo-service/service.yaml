apiVersion: v1
kind: Service
metadata:
  name: soc-demo-service
  namespace: adaptive-soc
  labels:
    app.kubernetes.io/name: soc-demo-service
    app.kubernetes.io/part-of: adaptive-soc-ai-framework
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: soc-demo-service
  ports:
    - name: http
      port: 8080
      targetPort: http
